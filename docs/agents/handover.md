# 引き継ぎメモ

セッションをまたいで作業を再開するための記録。**新しいものを一番上に足す。**

- 1件の区切りは行頭の `## `。書式は `docs/agents/flow-map.md` の「引き継ぎメモ」に従う。
- 保存の上限と、会話開始時に読み込む件数は `.claude/hooks/session-start.sh` が持つ。
- 追記したら `sh .claude/hooks/handover-trim.sh` を実行して件数を整える。

---

## 2026-09-23 検知の修正を本番に取り込み、本番で件数を比べた

**決めたこと**

- 利用者の指示「直せよ。ダメならGitで戻せばいい」により PR #45 を本番に取り込んだ。
- 同じ記事（口コミの解説、3段落）を本番で流して比べた。取り込み前1回: 主張12件・▶8件。取り込み後2回: どちらも主張12件・▶7件。取り出される主張の数は元に戻った。▶に出るべき指摘はすべて本文の印に乗り、黙って消えたものは0件。
- ▶が1件少ない理由は判定の揺れ: 「SIPSは2011年に電通が発表」が「一部食い違い」から「資料と一致」に変わった（2回とも）。事実確認・JEVまわりのコードは変更前と差分ゼロなので、拾ったページの違いによる揺れと判断した（層3）。
- AIっぽい表現の指摘（取り込み前は1件）は、指示どおり削除したので出ない。

- 利用者の記事（口コミが購買行動に与える影響、1844字・3ブロック）: 利用者の実行では▶8件と「一部の確認ができませんでした」。同じ記事を私が本番で流すと、失敗0件・主張25件・▶17件（すべて本文の印に乗った）。件数の差は、利用者の実行でどこかのサービスが失敗したためと見られる（層0か3）。どのサービスかは、私が失敗の中身の表示を消していたため分からなかった。表示を戻した（畳んで、サービス名とエラー文を出す）。

**次にやること**

- 「ご自身で確かめる」の検索語は、記事全体用の検索語6件をつないだ1本になっていて使えなかった（画面写真で確認）。その文そのもの（記号を除く）を検索語にするよう直し、本番に反映した。
- 次に「一部の確認ができませんでした」が出たら、開いてサービス名とエラー文を確かめ、層を特定する。
- 取り出した主張の文面が英語になっているものがある（生成側の問題、層1。変更前から）。記事の言葉で出させるか検討する。
- 同じ記事を数回流し、判定の揺れ幅を測る。

**未解決の問題**

- 同じ記事でも回ごとに拾うページが変わり、判定と数値が揺れる。
- 比べたのは1記事だけ。

## 2026-09-23 【記録】検知が大幅に減った。原因は私の変更。取り出し指示を元に戻した

**決めたこと**

- 利用者の指摘「表示しないことと感知能力の低下は別。回線を遮断しただろ」。サブ2体で原因を調べ、次を確定した。JEVの呼び出しと判定の手順は一切変えていなかった。
  1. 同じ文に複数の指摘が乗り、食い違いの印が文全体を覆うと、他の指摘が印から抜け落ちていた（昔からの不具合）。取り出し指示に足した「1文をまるごと抜き出す」がこれを踏みやすくし、新しい▶は印の無い指摘を黙って出さなかった。再現: 出すべき11件→8件。
  2. 同じ条件で、JEVに辻褄を聞くとき記事から抜く範囲が文まるごとに広がり、JEVに渡る材料が減っていた（ADR-0007違反）。
  3. 取り出す主張の数そのものが減った可能性（未確認）。
- 対処: 取り出しの指示を変更前（59e8e26）と完全に同じに戻した。人向け検索語（checkQueries）はやめ、確認に使った検索語を出す。抜け落ちを直し、印に乗らない指摘は必ず場所不明として▶に出す（黙って落とさない不変条件をテストで固定）。
- AIっぽい表現の検出を削除した（利用者の指示）。JEVへの文体ルールの問い合わせは走らない。事実確認の処理は差分ゼロ。
- 反省: ADR-0007と経緯記録（handover-archive）を読まずに、JEVの手前の入り口を変えた。本番で確かめずに提出した。

**次にやること**

- 配備後、嘘を仕込んだ試験記事（F01〜F03）で、本番（変更前）と試し版（変更後）の検知件数を比べる。試し版は外から開けないため、利用者の操作が要る。
- 取り出し・JEVへの渡し方を変えるときは、必ず ADR-0007 と handover-archive を先に読む。

**未解決の問題**

- 同じ記事でも回ごとに拾うページが変わり、数値が揺れる。

## 2026-09-23 吹き出しを「次の行動」だけにし、確かめるための検索語を出す

**決めたこと**

- 吹き出しから「この文」（▶で位置は示している）、大きな数値（▶に常時出ている）、説明文、「根拠の探し方」を削除した。残すのは判定・根拠のページ・「ご自身で確かめる」の3つ。文体の指摘だけは直し方の目安（説明文）を残す。
- 主張の抽出時に、人がGoogleにそのまま貼れる検索語（checkQueries、1〜2件）も作らせる。呼び出し回数は増えない。一次情報に当たる site: を使い、推測でドメインを作らない、疑わしい数値は入れない、と指示した。吹き出しでは押すとGoogleの検索結果が開く。
- 古い結果など checkQueries が無いときは、確認に使った検索語を代わりに出す。

**次にやること**

- 取り込み後、本番の実データで検索語の質（一次情報に届くか、site: が正しいか）と、場所不明の残り件数を確認する。
- 仕込んだ嘘の見逃し率を測る仕組み（利用者の了承待ち）。

**未解決の問題**

- 出せる直リンクは「検索結果」までで、一次情報のページそのものではない（見つかった根拠のページは別に出している）。
- site: のドメインが正しいかは生成側の知識に頼っている。

## 2026-09-23 場所不明の原因を直し、行動につながらない表示を整理

**決めたこと**

- 論者3人（出す意味がない／行動できない表示は消す／消さず解決策）に検証させ、全員が「まず照合を直す」で一致した。
- 原因: 本文との照合に、記事から抜き出した文（originalText）が一度も使われず、言い換え（normalizedText）だけで照合していた。抜き出した文で先に照合し、だめなら言い換えで照合するよう直した。抽出の指示も「元の1文を一字一句そのまま」に強めた（Anthropic・OpenAI の両方）。
- 表示の整理（原則「読んで次の行動に移れない表示は出さない」）: 「最終保存」を削除、文字数は本文の上の1か所だけ、サービスの状態の生エラー文は「一部の確認ができませんでした」と「もう一度確かめる」ボタンに置き換え。入力画面の「使い方を見る」も削除。

**次にやること**

- 取り込み後、本番の実データで場所不明が何件残るかを数える（以前は17件中3件）。
- ほぼゼロなら、場所不明は画面に出さない（論者A案）。まだ残るなら「本文と対応づけられなかった主張です」と明記し、とれる行動を2つに絞って書く（論者C案）。
- 論者Bが挙げた残り: 「根拠の探し方」の件数の内訳は開発者向けに移す候補。
- 仕込んだ嘘の見逃し率を測る仕組み（利用者の了承待ち）。

**未解決の問題**

- 照合の修正が効くかは、生成側が本当に原文のまま返すかにかかっており、実データで未確認。
- 同じ記事でも回ごとに拾うページが変わり、数値が揺れる。

## 2026-09-23 結果画面を「左の余白に▶、押すと吹き出し」に作り直し、名前を Proofee に

**決めたこと**

- 右カラム（1件ずつめくる形）と、スマホの下から出る画面を廃止した。画面写真で、一覧が無い・スマホで場所不明に行き着けない・数値が2つ並ぶ、と確認したため。
- 本文は右、一切変えない。左の余白に、指摘のある文の高さに合わせて「▶ 数値%」を置く。押すと吹き出しが開き、×を押すまで開いたまま。重なったら本文で上のものが手前。吹き出しは▶の列の左に開くので、▶は常に押せる。
- 場所不明の指摘は余白の一番上に「場所不明」としてまとめる。同じ行の指摘は▶を横に並べる。資料と一致した指摘は▶を出さない。
- 吹き出しの数値は1つだけ（信頼度の行と帯ラベルは出さない）。根拠は全件をリンクで出す（「根拠」と「根拠の探し方」の重複を解消）。
- 上部バーは名前だけにした。使い方・履歴・下書き・接続情報を削除。名前は Proofee（「ee」だけ青）。

**次にやること**

- 提出した変更を取り込み、本番の実データで▶の高さと吹き出しを確認する。
- 仕込んだ嘘の見逃し率を測る仕組み（利用者の了承待ち）。
- 書籍ライターの要望「確認済み／保留の印と出典メモ」は候補として保留。

**未解決の問題**

- 開いた吹き出しが下の吹き出しを隠す（利用者の指定どおり）。多く開くと下のものが読めない。
- 結びつけの基準は変えていないので、場所不明の件数は減っていない。
- 同じ記事でも回ごとに拾うページが変わり、数値が揺れる。

