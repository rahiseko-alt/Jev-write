# 引き継ぎメモ

セッションをまたいで作業を再開するための記録。**新しいものを一番上に足す。**

- 1件の区切りは行頭の `## `。書式は `docs/agents/flow-map.md` の「引き継ぎメモ」に従う。
- 保存の上限と、会話開始時に読み込む件数は `.claude/hooks/session-start.sh` が持つ。
- 追記したら `sh .claude/hooks/handover-trim.sh` を実行して件数を整える。

---

## 2026-09-23 どの文にも結びつかない指摘を「場所不明」として先頭に出す

**決めたこと**

- 書き手3人（記者・企業記事の受託・書籍）に評価させた。3人とも「当たりを付ける道具止まり」、うち2人が一番の改善点に「場所不明」の表示を挙げた。利用者は案②を選んだ。
- 印を付けるはずなのに本文のどの文にも結びつかない指摘は、数値に関わらず一覧の先頭に出し、「場所不明」の注意と「主張の文面（本文の言葉とは異なります）」を表示する。資料と一致した指摘は元々印を付けないので対象外。
- 画面に残っていた書き換え時代の説明（入力欄・使い方・案内の3か所）を「書き換えない」に直した。

**次にやること**

- 提出した変更を取り込み、本番で場所不明の指摘が先頭に出ることを確認する。
- 右カラムの無意味4つの削除と冗長3つの初回限定表示（前回の棚卸し）は、まだ利用者の了承を得ていない。了承を得てから行う。
- 書籍ライターの要望「確認済み／保留の印と出典メモを残し、次回に引き継ぐ」は候補として保留。

**未解決の問題**

- 結びつけの基準そのものは変えていないので、場所不明の件数は減っていない。
- 同じ記事でも回ごとに拾うページが変わり、数値が揺れる。

## 2026-09-23 書き換え停止を本番で確認。右カラムの棚卸し

**決めたこと**

- 本番で確認済み: 出力は原文と完全に一致（入力末尾の空行が落ちるのみ）。ブロック1の所要は44秒→33秒に短縮。画面から「修正版／左右比較／インライン差分」「採用／元に戻す」は消えている。
- 右カラム16項目を棚卸しした。**無意味（重複）4つ**: 右上の丸バッジ、「記事の中での筋の通り方」の再掲、「種別」行、信頼度のバー。**冗長3つ**: 「低いほど確かめる値打ち」「数値はJEVが返したもの」「末尾の注意枠」（初回だけでよい）。ほかに「この文」の赤背景は誤りを含意するので中立色にすべき、「根拠」1件表示は「根拠の探し方を見る」に統合すべき。
- 本文の印と右カラムの件数が食い違う理由を実データで確認（指摘17件→印5か所）: 資料と一致した文は印を付けない設計で9件、どの文にも結びつけられなかったものが3件、同じ文に重なったものが1文。

**次にやること**

- 利用者の返事を待っている選択: 結びつかなかった指摘の扱いを ①結びつけの基準を緩める ②一覧の先頭に「場所不明」として明示する のどちらにするか（おすすめは②）。
- 決まったら、右カラムの無意味4つの削除と冗長3つの初回限定表示もあわせて実施する。

**未解決の問題**

- 指摘の3件に1件程度が本文の文に結びつかない（言い換えの度合いが大きいもの）。
- 同じ記事でも回ごとに拾うページが変わり、数値が揺れる。
- 実数が公表されていない主張は、数値で「裏付けなし」と示すところまで。

## 2026-09-23 書き換えをやめた（ADR-0010）

**決めたこと**

- 文章を一切書き換えない。裏付けの弱い箇所を数値で知らせるだけにする。利用者の指示: 「もう一切書き換えなくていいよ。信頼度の低いところを検知して知らせろよ」。
- 書き換えのための部品をこの置き場所から削除した: 書き換え依頼（rewrite-pipeline）、差分検査（delta-check）、局所修正（surgicalFix）、訂正文の作成（correction）、数値の抽出（figures）、ファクト台帳（FactLedger）、JEVへの差分判定の問い合わせ。
- 画面は一つにした。原文／修正版／左右比較／インライン差分の切り替えと、採用・元に戻すの操作を削除。出すのは「裏付けの強さの数値・印・根拠のページ」の3つだけ。
- 文体の指摘は検知して知らせるだけで、直さない。

**次にやること**

- 配備後、記事全体を通して画面を確認する（印・数値・全文コピーが原文のままであること）。
- 処理時間が短くなったかを測る（生成1回とJEV1回が減っている）。

**未解決の問題**

- 同じ記事でも回ごとに拾うページが変わり、数値が揺れる。
- 実数が公表されていない主張は、数値で「裏付けなし」と示すところまで。

## 2026-09-23 記事全体を通しで仕上げた（PR #41・#42・#43）

**決めたこと**

- 指摘を本文の文に結びつける方法を、文字列の一致から「共有する言葉（2文字の連なり）の割合」に変えた。主張は記事の引用ではなく言い換えなので、一致では全件が行方不明になっていた。半分未満しか共有しない主張は、どこにも貼り付けない。
- 本文の印を「断られた修正案」の薄い表示にするのは、採否を選べる修正案だけ。裏付けが無い文ははっきりした印で出す。
- 本文の印の見出しを「裏付けが見つかりません」に統一した（PR #43。取り込み待ち）。

**本番での通し確認（記事1,653字）**

- 3ブロックに分割 → 順に処理 → 統合 → 全文コピーまで動作。所要123秒、指摘33件、失敗0件。
- 本文の印11件がすべてはっきりした表示で出る。数値は低い順（2%〜）に並ぶ。
- 修正案は既定で本文に入らない（原文のまま出る）。

**次にやること**

- PR #43 を取り込む。
- 同じ記事を数回流し、数値と拾うページの揺れ幅を測る。

**未解決の問題**

- 同じ記事でも回ごとに拾うページが変わり、数値が揺れる。
- 実数が公表されていない主張は、数値で「裏付けなし」と示すところまで。
- 文体書き換え機能は凍結のまま。

## 2026-09-23 誤検知0件になった（PR #40）

**決めたこと**

- 違うことを書いているページが1つのサイトにしか無い場合は「食い違い」と呼ばない。別々のサイト2件以上が同じ方向を示したときだけ「資料と食い違い」とする。読み方の規則は `src/lib/pipeline/evidence-reading.ts` に切り出した。

**本番実測（ブロック1、44秒、検索6回）**

- **食い違いの札は0件。正しい文を誤って咎めることはなくなった**（前回の「299ライブプロダクションが運営」は1サイトのみのため「裏付けなし 5%」に下がった）。
- 仕込んだ捏造3件は、いずれも低い数値で上位に並んだ: 5％徴収 8%、約6割 14%、142人 53%（3倍 63%）。
- 正しい文は72〜93%。数値で上下がはっきり分かれている。

**次にやること**

- 同じブロックを数回流して、数値の揺れ幅を測る（拾うページが回ごとに変わるため）。
- ブロック2・3でも同じ形になるか確認する。

**未解決の問題**

- 同じ記事でも回ごとに拾うページが変わり、数値が揺れる。
- 実数が公表されていない主張は、数値で「裏付けなし」と示すところまで。
- 原文と修正文の対応付け、文体書き換えは凍結のまま。

