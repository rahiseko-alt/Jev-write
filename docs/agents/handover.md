# 引き継ぎメモ

セッションをまたいで作業を再開するための記録。**新しいものを一番上に足す。**

- 1件の区切りは行頭の `## `。書式は `docs/agents/flow-map.md` の「引き継ぎメモ」に従う。
- 保存の上限と、会話開始時に読み込む件数は `.claude/hooks/session-start.sh` が持つ。
- 追記したら `sh .claude/hooks/handover-trim.sh` を実行して件数を整える。

---

## 2026-09-23 記事全体を通しで仕上げた（PR #41・#42・#43）

**決めたこと**

- 指摘を本文の文に結びつける方法を、文字列の一致から「共有する言葉（2文字の連なり）の割合」に変えた。主張は記事の引用ではなく言い換えなので、一致では全件が行方不明になっていた。半分未満しか共有しない主張は、どこにも貼り付けない。
- 本文の印を「断られた修正案」の薄い表示にするのは、採否を選べる修正案だけ。裏付けが無い文ははっきりした印で出す。
- 本文の印の見出しを「裏付けが見つかりません」に統一した（PR #43。取り込み待ち）。

**本番での通し確認（記事1,653字）**

- 3ブロックに分割 → 順に処理 → 統合 → 全文コピーまで動作。所要123秒、指摘33件、失敗0件。
- 本文の印11件がすべてはっきりした表示で出る。数値は低い順（2%〜）に並ぶ。
- 修正案は既定で本文に入らない（原文のまま出る）。

**次にやること**

- PR #43 を取り込む。
- 同じ記事を数回流し、数値と拾うページの揺れ幅を測る。

**未解決の問題**

- 同じ記事でも回ごとに拾うページが変わり、数値が揺れる。
- 実数が公表されていない主張は、数値で「裏付けなし」と示すところまで。
- 文体書き換え機能は凍結のまま。

## 2026-09-23 誤検知0件になった（PR #40）

**決めたこと**

- 違うことを書いているページが1つのサイトにしか無い場合は「食い違い」と呼ばない。別々のサイト2件以上が同じ方向を示したときだけ「資料と食い違い」とする。読み方の規則は `src/lib/pipeline/evidence-reading.ts` に切り出した。

**本番実測（ブロック1、44秒、検索6回）**

- **食い違いの札は0件。正しい文を誤って咎めることはなくなった**（前回の「299ライブプロダクションが運営」は1サイトのみのため「裏付けなし 5%」に下がった）。
- 仕込んだ捏造3件は、いずれも低い数値で上位に並んだ: 5％徴収 8%、約6割 14%、142人 53%（3倍 63%）。
- 正しい文は72〜93%。数値で上下がはっきり分かれている。

**次にやること**

- 同じブロックを数回流して、数値の揺れ幅を測る（拾うページが回ごとに変わるため）。
- ブロック2・3でも同じ形になるか確認する。

**未解決の問題**

- 同じ記事でも回ごとに拾うページが変わり、数値が揺れる。
- 実数が公表されていない主張は、数値で「裏付けなし」と示すところまで。
- 原文と修正文の対応付け、文体書き換えは凍結のまま。

## 2026-09-23 ページの選び方を回数で決めた（PR #39）。誤検知は2件→1件

**決めたこと**

- ページの並びを、主語に触れている回数で決める（1語あたり上限6回）。1文あたりの候補を5→8、1検索あたりの取得を4→7に増やした。検索回数は6回のままで費用は増えない。
- 説明文の英語（「JEVの判定: contradicts」）を日本語に直した。

**本番実測（ブロック1、42秒、検索6回）**

- 徒歩8分・徒歩3分の誤検知は解消（SUPPORTED 91%、根拠はこくちーずのフリノバ紹介ページ）。
- 新たな誤検知1件: 「現在は合同会社299ライブプロダクションが運営している」を食い違い（辻褄6%）。根拠はレンタルスペース掲載サイト（instabase）2ページで、どちらも同じドメイン。掲載ページはフリノバを何度も書いているため回数では上位に来る。
- F03「5％徴収」はこの回は候補8件すべて不採用で「裏付けなし」（9%）。回ごとに拾うページが変わるため、検知が出る回と出ない回がある。

**次にやること**

- 食い違いの札は「**別々のドメイン2件以上が同じ方向を示したとき**」に限り、1ドメインだけなら「裏付けなし（食い違うページあり）」として数値と根拠だけ見せる案を検討する。
- 同じブロックを複数回流し、判定の揺れの幅を測る。

**未解決の問題**

- 掲載サイトや予約サイトが、主語を何度も書くために上位に来る。
- 同じ記事でも回ごとに拾うページが変わり、判定が揺れる。
- 実数が公表されていない主張は、数値で示すところまで。
- 原文と修正文の対応付け、文体書き換えは凍結のまま。

## 2026-09-23 「誤り」と断定するのをやめた（ADR-0009）

**決めたこと**

- 判定の札を真偽ではなく資料との関係に変えた。「事実の誤り」→**「資料と食い違い」**、「確認済み」→**「資料と一致」**、「根拠が見つかりません」→**「裏付けなし」**（誤りという意味ではないと明記）。理由は `docs/adr/0009-no-verdicts.md`。
- 事実の修正案は提案にとどめ、**既定では本文に採用しない**。利用者が選んで初めて入る。文章表現の修正は従来どおり既定で採用する。
- 「誤り」という語は画面から消える。「何件検知したか」で測るのもやめる。評価の軸は、裏付けの強さの数値が、確かめる値打ちのある文を上に並べられているか。

**この決定に至った経緯（利用者の指摘）**

> 間違っているものを間違っていると判断するツールじゃないだろ

> 誤りと、断定した、の意味が分からない。なぜ断定という結果を出すんだ？

私の回答:「断定しているのは私の作りが古いままだからです。判定の札が4種類あり、資料が食い違えば自動で『事実の誤り』と表示し、修正案まで出す——今日決めた『正誤を宣告しない』方針と矛盾しています。実際に起きているのは『あるページが違うことを書いている』という事実だけで、どちらが正しいかは分かりません。」

きっかけは本番実測で、公式サイトに載っている正しい文（「国際センター駅から徒歩3分」「名古屋駅から徒歩8分」）を、別施設の掲載ページを根拠に「誤り」と断定したこと。

**次にやること**

- 配備後、同じ3ブロックを流し、札の文言と、修正案が既定で入らないことを画面で確認する。
- 1文あたりの候補ページ数と1検索あたりの取得件数を増やし、公式ページが埋もれないようにする（検索回数は6回のまま）。

**未解決の問題**

- 資料の集め方が記事全体向けになったぶん、特定の文にだけ必要なページが落ちやすい。
- 実数が公表されていない主張は、食い違いとしても示せない。数値で「裏付けなし」と示すところまで。
- 原文と修正文の対応付け、文体書き換えは凍結のまま。

## 2026-09-23 検索を1/6に削減（PR #37）。ただし誤検知2件

**決めたこと**

- 検索は記事ごとに1回だけ行い、見つけたページを全文で使い回す（`src/lib/pipeline/source-pool.ts`）。本番で確認: **1ブロック34回 → 6回**。所要時間は44秒/35秒/27秒で、以前と同等か短い。
- 利用者がTavilyを従量課金（Pay as you go）に切り替えたため、検証は継続できる。

**本番実測（ブロック1〜3、検索6回ずつ、失敗0）**

- F03「5％徴収」: 矛盾として検知（辻褄4%）。F02「約6割」: 13%で要確認。F01「142人」: 44%で要確認。
- **誤検知2件**: 「国際センター駅から徒歩3分」（辻褄3%・矛盾）、「名古屋駅から徒歩8分」（辻褄8%・矛盾）。どちらも公式サイトに載っている正しい文。資料集めが記事全体向けになったため、公式のアクセスページが候補から落ち、別施設の掲載ページだけが残ったことが原因とみられる。

**次にやること**

- 誤検知2件への対処として、1主張あたりの候補数と1検索あたりの取得件数を増やし、公式ページが埋もれないようにする（検索回数は変わらない）。
- その後、同じ3ブロックで誤検知が消えるか確認する。

**未解決の問題**

- 資料の集め方が記事全体向けになったぶん、特定の文にだけ必要なページ（アクセス・料金など）が落ちやすい。
- 実数が公表されていない主張は、矛盾として断定できない。
- 原文と修正文の対応付け、文体書き換えは凍結のまま。

