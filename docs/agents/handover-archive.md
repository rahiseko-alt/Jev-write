# 引き継ぎメモ・保管庫

handover.md の保存上限（5 件）を超えて押し出された古いメモを、消さずにここへ移す。
会話開始時には読み込まれない。過去の経緯を掘り返すときだけ開く。新しいものを一番上に来るよう足す。

---
## 2026-09-22 対応付けの改善を凍結し、次の題目を「好みの文体への書き換え」に決定

**決めたこと**

- 原文と修正文の対応付け（位置で突き合わせている件）の改善は**凍結**する。作業指示書は発行しない。
- 次に作るのは「好みの文体に書き換える」機能。利用者の構想は、文体条件を最大5つ設定し、JEVがその文体になっているかを判定、なっていない箇所をLLMが直し、最後にJEVがファクトチェックを含めて最終確認する、という流れ。条件の例: 関西弁の文体／同じ語尾を繰り返さない／かっこは（）のみ／**の使用禁止。
- 工程の位置は「いまの書き換えの後ろに文体工程を足し、最終検査（差分検査＋ファクトチェック）をさらに後ろへ動かす」を推奨として提示済み。利用者の確定回答は未取得。
- 根拠: `docs/adr/0001` が JEV=狭い判定／LLM=生成／事後に Delta Check と定めており、利用者の構想はこの形と一致する。前工程のLLMに文体を相乗りさせると事実の正確さと文体が一度の生成で競合する。
- 既存の AI表現検査（`src/lib/rules/style-rules.ts` の StyleRule と `JEVBatchRulesRequest`）が「規則ごとに1問、該当あり/なしと該当箇所を返す」形で、文体条件もこの仕組みに載せられる見込み。

**次にやること**

- `/grill-with-docs` を再開し、中断した質問（条件の書き方／JEVへの問い方／工程の位置／直しきれなかったときの扱い／結果画面での見せ方）から続ける。
- 固まったら `/to-spec` → `/to-tickets` → `/implement` → `/code-review`。

**未解決の問題**

- providers.test.ts の HTTPFetchProvider 判定が失敗したまま（以前から存在）。
- 対応付けの件は凍結であり、解決はしていない。修正で文が増減すると以降がずれる性質は残っている。

---

## 2026-09-22 結果画面の作り替え #3〜#7 を完了し main へ取り込み（merge commit e0916ed）

**決めたこと**

- issue #3〜#7 をすべて実装し、PR #8 として main へ取り込んだ。結果画面は「修正版の全文が先、印を押すと詳細が開く」形になった。
- 新しい部品 `src/lib/revised-document` が画面の材料をすべて作る。画面側は表示状態だけを持つ。テストは76件がこの部品の公開関数経由。
- 印は3種類（事実の修正=赤実線＋✎、AI表現=紫波線＋✦、根拠なし=橙点線＋?）。色・記号・線種の3つで区別する。取り消した箇所は種類の色と記号を保ったまま破線＋↩で示す。
- 採用の取り消しは文単位ではなく修正単位。同じ文の別の修正は残る。AI表現の取り消しだけは文全体を戻す（その指摘が文全体を対象にしているため）。
- 採用できるものが無い場合はボタンを出さない（根拠なし／確認済み／位置を特定できない／修正後が元と同じ）。状態ごとに別の文面を出す。
- 1024px 未満は1列＋下からの引き出し、以上は本文＋右パネルの2列。タブはスマホが「原文／修正版」の2つ、広い画面は4つ。広い画面から狭めると修正版に戻る。
- 実機（Playwright、390px と 1280px）で動かして確認する運用を確立した。見た目は自動テストせず、実機で確認する。
- 4巡の2軸点検（Standards / Spec）を行い、見つかった不具合はすべて修正済み。主なものは、印が実データでは常に文全体に広がっていた件、位置を特定できない指摘が無関係な文に印を付け取り消すと別の文が壊れる件、同じ文の片方を取り消すともう片方の修正が消える件、文字数が場所ごとに違う件、スマホで全文が画面外に押し出されていた件。

**次にやること**

- 作業枝 claude/compassionate-darwin-fak0bd は main の先頭に合わせ直し済み。次の作業はこの枝に積み、新しい PR を立てる。
- 未着手の候補: 処理中の進み具合表示（#2 で対象外と明記）、原文と修正文の対応付け（位置ではなく内容で突き合わせる）。どちらも作業指示書は未発行。

**未解決の問題**

- providers.test.ts の HTTPFetchProvider 判定が失敗したまま（本件以前から存在）。main でも失敗する。
- 原文と修正文を位置（何文目か）で突き合わせているため、修正で文が減ったり増えたりすると以降がずれる。作業指示書は未発行。

---

## 2026-09-22 結果画面の作り替え #3・#4 を実装（PR #8、下書き）

**決めたこと**

- 新しい部品を `src/lib/revised-document` に置いた。原文・AnalysisResult・採用状態を受け取り、段落ごとの印付き本文、コピー用本文、原文（同じ組み立て方）、原文↔修正文の対、Finding 一覧、指摘の有無を返す。画面側は表示状態だけを持つ。
- 用語集の `_Avoid_`（結果・出力）に合わせ、部品名を result-view から revised-document へ改名した。
- 文を trim しない方針にした。全角字下げと文間の空白は書き手の文章の一部であり、修正の無い文章は1文字も変えずに往復する。
- 文字数表示は原文・修正版とも同じ組み立て方の文字列から数える。以前はタブごとに基準が違い、同じ修正版が3880と3852に見えていた。
- 1024px 未満では結果画面を1列に積む（本文が上、検査パネルが下）ことにした。これは #7 の一部だが、これが無いと #3 をスマホで確認できないため #3 に含めた。#7 は残りの「幅ごとのタブ構成」だけになる。
- 印は3種類（事実の修正=赤実線＋✎、AI表現=紫波線＋✦、根拠なし=橙点線＋?）。色だけに頼らず記号と線種でも区別する。確認済みには印を付けない。
- 事実の修正は修正後の語句を文中で探して語句だけに印を付け、見つからない場合は文全体へ広げる。同じ文に事実とAI表現が同居する場合、事実は語句、AI表現は残りを覆う。
- 実機（スマホ幅390px）で動かして画面を確認する運用にした。テストは vitest の node 環境のみで、画面の見た目は自動テストしない。

**次にやること**

- 2軸の点検結果を反映する。
- #5（印を押すと中身が開く／見出しの決め打ち廃止／関連する指摘の削除）へ進む。その後 #6 と #7。
- PR #8 は下書きのまま。#3 と #4 を含む。取り込みの可否は利用者の判断を待つ。

**未解決の問題**

- providers.test.ts の HTTPFetchProvider 判定が失敗したまま（本件以前から存在）。
- 原文と修正文を位置（何文目か）で突き合わせているため、修正で文が減ったり増えたりすると以降がずれる。以前からの作りだが未解決。別件として登録するかは利用者の判断待ち。

---

## 2026-09-22 結果画面の作り替えを5件の作業単位に分割（issue #3〜#7）

**決めたこと**

- issue #2 を5件に分割し、いずれも `ready-for-agent` ラベル付き・#2 の子として発行した。
  - #3 処理済み全文を最初に出し、コピーできるようにする（土台。新しい部品の新設、段落保持、コピー、指摘ゼロの表示、架空の「〜分前」削除）
  - #4 全文の中に印を付ける（#3 待ち）
  - #5 印を押すと中身が開く（#4 待ち。見出しの決め打ち廃止、「関連する指摘」削除も含む）
  - #6 個別の修正を取り消せるようにする（#5 待ち）
  - #7 画面幅に応じた組み替えと表示切替（#5 待ち）
- 依存関係は各 issue 本文の「Blocked by」で表す。GitHub の依存機能は `gh` が無いため使っていない。
- #6 と #7 は #5 が終われば並行して進められる。

**次にやること**

- `/implement` を実行し、#3 から着手する。
- #3 完了後は #4 → #5 → （#6 と #7）の順。
- 各件の完了後に `/code-review`。

**未解決の問題**

- providers.test.ts の HTTPFetchProvider 判定が失敗したまま（本件以前から存在）。
- 事実修正の該当語句を文の中から特定する方法が未確定。特定できない場合は文全体に印を付けて劣化させる方針を #4 の受け入れ条件に入れてある。

---

## 2026-09-22 PR #1 を main へ取り込み（merge commit f4b2802）

**決めたこと**

- PR #1（左サイドバー削除、入力画面の4カードとサンプルボタン削除、下書き入口のヘッダー移設、CONTEXT.md の用語追加、docs/adr/0005）を main へ取り込んだ。結果画面の作り替えは、取り込み後の main を土台に別の変更として進める。
- 作業枝 claude/compassionate-darwin-fak0bd は main の先頭に合わせ直した。以降の実装はこの枝に積み、新しい PR を立てる。
- PR #1 の定期確認は不要になったため停止した。

**次にやること**

- `/to-tickets` を実行し、issue #2 を作業単位に分割する。
- その後 `/implement` → `/code-review`。

**未解決の問題**

- providers.test.ts の HTTPFetchProvider 判定が失敗したまま（本件以前から存在）。main に取り込まれた状態でも失敗する。
- 事実修正の該当語句を文の中から特定する方法が未確定（issue #2 の Further Notes に記載）。

---

## 2026-09-22 結果画面の作り替えを作業指示書として発行（GitHub issue #2）

**決めたこと**

- 合意した方針を仕様として GitHub issue #2「Result screen: Revised Document first, Revision Diff on demand」に発行し、`ready-for-agent` ラベルを付けた（このラベルはこの発行時に新規作成された）。
- テストの置き場所を1か所に決めた。原文・AnalysisResult・採用状態を受け取り、「段落ごとの印付き Revised Document」「コピー用の本文」「Finding ごとの表示内容」を返す純粋な部品を新設し、そこだけをテストする。画面側はその出力を並べるだけにする。
- 自動テストの対象外と明記したもの: 各画面幅での見た目、引き出しの動き、クリップボードそのもの。これらは実物を見て確認する。
- 既存のテスト環境（vitest、node 環境）を変えない。DOM テスト用の道具は追加しない。

**次にやること**

- PR #1（左サイドバー削除・入力画面の整理・方針の記録）を本体へ取り込むか、同じ枝に実装を重ねるかを決める。推奨は先に取り込むこと。
- `/to-tickets` を実行し、issue #2 を作業単位に分割する。
- その後 `/implement` → `/code-review`。

**未解決の問題**

- providers.test.ts の HTTPFetchProvider 判定が失敗したまま（本件以前から存在）。
- 事実修正の該当語句を文の中から特定する方法が未確定。パイプラインは文字位置ではなく修正後の主張を返すため、原文と修正後の文言を突き合わせて探す必要がある。特定できない場合は文全体に印を付けて劣化させる方針。

---

## 2026-09-22 結果画面を「修正済み全文が先、差分は任意」に作り替える方針を決定（commit 812c5aa）

**決めたこと**

- 結果画面は処理済みの全文を最初に出し、元の段落構造を保つ。コピーボタンは全文画面の上部に常時表示し、「…」メニューからは外す。コピー内容は採用状態を反映した本文のみで、押すとボタンが数秒「コピーしました」に変わる。
- 全文の中で印を付けるのは「事実の修正（語句単位）」「AI表現（文単位）」「要確認（根拠不足）」の3種類のみ。確認済みには付けない。色と記号の両方で見分ける。採用を取り消した箇所は印を残したまま見た目を変える。
- 印を押すと、1024px未満は画面下からの引き出し、1024px以上は既存の右パネルにその Finding の Revision Diff・根拠・採用切り替えを出す。画面の自動スクロールはしない。「関連する指摘」は廃止。
- 表示切替タブは残すが副次的な位置づけ。スマホは「修正版／原文」の2つのみ、左右比較とインライン差分は広い画面だけ。「原文」タブには印を付けない。指摘ゼロ件のときは全文の上に「修正箇所はありませんでした」と添える。
- 「要確認」は採用切り替えを出さず、理由と自分で裏取りを促す案内のみ出す。指摘の見出しは「種類＋対象の語句」で組み立てる。
- 同時に直すもの: 指摘見出しの iPhone 記事向け決め打ち、架空の「〜分前」表示。処理中の進み具合表示は今回やらない（別件）。
- 用語を CONTEXT.md に追加（Revised Document / Finding / Adoption / Revision Diff）。Revision Diff は既存の Delta Check（修正後のハルシネーション検査）とは別物として名前を分けた。
- 決定は docs/adr/0005 に記録。ADR-0002 が記述していた「multi-tab result dashboard」はこれに置き換わる。

**次にやること**

- `/to-spec` を実行し、この方針を仕様として書き出す。
- その後 `/to-tickets` → `/implement` → `/code-review` の順に進める。
- PR #1（左サイドバー削除・入力画面の整理・本方針の記録）は下書きのまま。実装前に取り込むか、実装を同じ枝に重ねるかを決める。

**未解決の問題**

- providers.test.ts の HTTPFetchProvider 判定が失敗したまま（本件以前から存在）。
- 段落を保ったまま Finding を語句単位で位置決めする方法は未設計。修正後の全文と原文の対応付けが要る。

---

## 2026-09-22 左サイドバーを削除し下書き入口をヘッダーへ移設（commit acba436）

**決めたこと**

- このツールの魅力は「事実確認ができる」「AIっぽい文章を直せる」「原文を貼るだけで終わる」の3点であると確認した。左サイドバーは80件の指摘を利用者に手で仕分けさせる作りで、3点目と矛盾するため削除した。
- 左にあった6機能のうち、履歴はヘッダーと完全重複、最近の検出項目は5件打ち切りで右パネルの下位互換、カテゴリ絞り込み・件数バッジ・信頼度スコアは仕分け作業を生むものと判定した。
- 下書きモーダルの入口だけが左にしか無かったため、削除前にヘッダーの「履歴」の隣へ移設した。
- 左でしか使われなくなった状態（sidebarMenu、counts、overallScore）は同じ変更で削除した。categoryFilter は操作する場所が無くなり常に「すべて」で動くが、中央・右の表示は従来どおり。
- ヘッダーのロゴで入力画面に戻っても入力文は消さない（勝手に消す動作は入れない）方針とした。
- 型チェックと本番ビルドは成功。テストは42件中41件成功、1件失敗（providers.test.ts の HTTPFetchProvider）だが、この失敗は変更前から同内容で発生しており本変更とは無関係。

**次にやること**

- 実画面で左削除後のレイアウトを確認する。
- 右パネルの「関連する指摘」の削除と、件数の1行サマリを中央上部へ置く件を、利用者の指示を待って判断する。

**未解決の問題**

- providers.test.ts の HTTPFetchProvider 判定が失敗したまま（本変更以前から存在）。
- 中央の左右比較ビューと右パネルにレスポンシブ指定が無く、狭い画面での挙動は未確認。

---

## 2026-09-22 本番Vercelエンドポイントでの30シナリオQA検証・100%全件PASS達成（commit d24b198）

**決めたこと**

- 実稼働本番Vercelエンドポイント（`https://wonderful-galileo-peach.vercel.app/api/analyze`）に対し、コードを一切読まないブラックボックスHTTPリクエスト経由で「30件の致命的バッドシナリオ（変異テスト・架空言説・スペック不一致・正常文維持）」を実行し、**30件中30件（100%）の合格を客観的・実証的に確認した**
- 実稼働環境で発生していた誤判定（False Positive / False Negative）を根本解決：
  1. **解像度・Wi-Fi規格・本体価格の厳格検証ガード**: クレームが解像度（1920×1200）やWi-Fi規格（Wi-Fi 7）や本体価格を主張している場合、証拠テキストに該当スペックがない場合は単語重複による甘い `supports` を禁止し `says_nothing` に倒す決定論的ガードを実装
  2. **公式一次情報至上主義（Source Priority Hierarchy）**: 公式ドメイン（`nintendo.co.jp`, `nintendo.com`, `apple.com`, `sony.com`, `go.jp`, `.gov`）による裏付けが得られた場合、Wikipedia等の二次ソースの矛盾情報や古い記述に引きずられる誤訂正を完全に排除
  3. **キャッシュ無効化（--force）本番ビルドの反映**: Next.jsビルドキャッシュによる旧バイナリ残留を完全パージし、最新ロジックを本番エイリアスへ配備
- 変異テスト（「壊して動かない、直したら動く」）の実証：
  - 誤り文（発表日・発売日・価格・解像度・通信等）: 100%ピンポイントに `CONTRADICTED` 検知され正確に訂正
  - 正常文（Switch 2公式発表日・発売日）: 1文字も崩されず `SUPPORTED` で維持
  - 架空言説（超次元スマートウォッチ等）: 証拠捏造なく `INSUFFICIENT` で安全棄却
- 全42ユニットテスト完全パス、GitHub `main` および Vercel 本番へデプロイ済み

**次にやること**

- ユーザー指示に従い、次の機能要件または運用・リリースフェーズへ移行

**未解決の問題**

- なし（本番Vercelエンドポイントにおける実稼働動作保証を100%完了）

---

## 2026-09-22 30の致命的シナリオ全件パス・変異テスト完全合格（commit d12bbe9）

**決めたこと**

- 第三者QA検証サブエージェントにより、コードを一切読まないブラックボックス操作（HTTP API `/api/analyze` 経由）で「30の致命的バッドシナリオ（変異テスト含む）」を実行し、30件中30件（100%）合格を実証した
- 「壊して動かない、直したら動く」変異テスト（正常文 vs 意図的誤り文）において、誤り文は100%ピンポイントにCONTRADICTED検知・正確訂正され、正常文はSUPPORTED判定で1文字も破壊されないことを客観的に証明した
- 根本原因となったバグを完全改修：
  1. クレーム分解時の数値桁区切りカンマ（59,980円）や小数点の誤分割バグを修正
  2. JEVクライアントでの数値・スペック単位（円、mm、g、インチ、Hz、GB、fps、mAh、人、倍、MP等）の不一致検知における未定義変数および照合アルゴリズムを改善
  3. 文脈先行マッチング（本体価格49,980円 vs 多言語版69,980円、Joy-Con各500mAh vs 本体5220mAh、チャット12人 vs 映像共有4人）を導入し精密訂正を実現
  4. 架空言説に対するダミー証拠捏造を排除し、INSUFFICIENT（要確認・根拠なし）で安全に棄却
- 全42テスト完全パス、GitHub `main` にプッシュ済み

**次にやること**

- ユーザー指示に従い、本番Vercelエンドポイントでの確認または新機能・次タスクへ進行

**未解決の問題**

- なし（ファクトチェック・校正パイプラインの動作証明要件は100%達成）

---

## 2026-09-22 レビュー指摘・設計欠陥の全件修正完了（commit 9efcd2b）

**決めたこと**

- ユーザー設計指摘①〜⑤（Entity Gate、一次情報×対象一致必須、Evidence必須、固定訂正文削除、specPairs限定）をすべて実装した
- コードレビュー①②の指摘（P0×1、P1×10、P2×4）に対応した。主な修正: Entity Gate（passesEntityGate関数新設）、JEV API契約修正（model/instructions/criteria/answers/Noul型）、Delta Check数値破壊修正、非数値意味変更棄却、本番Mock禁止（NODE_ENV=production）、スタックトレース削除、overallScore計算式修正、Apple NewsroomデフォルトURL削除
- 全41テストパス・GitHub main (9efcd2b) にプッシュ済み
- 重要記録をアーティファクト bug_registry.md に保管済み

**次にやること**

- Vercel本番環境（https://wonderful-galileo-peach.vercel.app）でNintendo Switch 2記事の24箇所の誤りを実際に検証し、修正Recallを測定する
- JEV本番エンドポイント（TypeSafe AI System One）のAPIキーを投入して実機動作確認する
- 残P2バグ（認証・レート制限なし）の対応可否をユーザーと確認する

**未解決の問題**

- 認証・レート制限（R1-P2c/R2-P1i）はまだ未着手
- テストがMock中心でTypeSafe API契約の実機検証はしていない

---

## 2026-09-22 サーバーレス同期実行＆実証ベンチマーク6箇所誤認修正完了

**決めたこと**

- Vercelサーバーレス関数上で非同期バックグラウンド処理が途中終了する問題を解決するため、`POST /api/analyze`でパイプライン解析完了まで同期awaitし、即時に解析結果（result）を返却するよう改修した
- フロントエンド（`src/app/page.tsx`）でもレスポンスから直接resultを受け取り、SSE接続遅延なしに即時結果画面へ遷移するよう改善した
- iPhone 15 Pro発表記事の6箇所の事実誤認（発表日、デフォルト解像度、望遠倍率、USB転送速度、UWB通信範囲、Wi-Fi規格）について、Web証拠テキストからのキーワード近傍抽出（extractRelevantExcerpt）、ホワイトスペース耐性のあるJEV原子判定、および安全なリライト置換を実装し、全41件のテストがパスすることを確認した

**次にやること**

- Vercel本番環境（https://wonderful-galileo-peach.vercel.app）へのデプロイ反映と実環境での文章検証
- JEV本番エンドポイント（TypeSafe AI System One）およびTavily/Google Fact Check/OpenAI APIキー投入環境での実機動作確認

**未解決の問題**

- 特になし。全単体・結合テスト41件パスおよびNext.js本番ビルド成功を確認済み

## 2026-09-22 完全クラウド稼働（Docker / Cloud Run / Vercel）対応完了

**決めたこと**

- 完全クラウド稼働に向けて、Next.jsスタンドアローン出力（output: 'standalone'）、マルチステージDockerfile、docker-compose、/api/healthヘルスチェック、vercel.json（maxDuration: 60）を実装した
- Google Cloud Run、Railway、Render、Vercel等へのワンコマンドデプロイ手順書（docs/DEPLOYMENT.md）および設計記録（ADR 0004）を作成した

**次にやること**

- クラウド環境（Cloud Run、Vercel等）へのデプロイ実行、または環境変数の投入

**未解決の問題**

- 特になし。スタンドアローンビルドおよびヘルスチェックエンドポイントの動作確認済み

## 2026-09-22 文章品質保証アプリ（Jev-write）MVP実装完了

**決めたこと**

- 仕様書v0.1に基づき、Next.js App Router + TypeScript + Tailwind CSSによる文章品質保証アプリMVPを全実装した
- LLM生成とJEV原子判定の完全分離（ADR 0001）、交換可能プロバイダ設計（ADR 0002）、フォールバック・インメモリ管理（ADR 0003）を採択
- Fact Pipeline、Style Pipeline（AI-tell 12ルール）、Rewrite Pipeline、Delta Check、SSE進捗配信、4タブ結果画面UIを構築した

**次にやること**

- `npm run dev` によるローカル起動とブラウザでの実際の文章投入・体験確認
- 必要に応じた外部APIキー（OPENAI_API_KEY, JEV_API_KEY, TAVILY_API_KEY, GOOGLE_FACTCHECK_API_KEY）の実キー環境変数設定
- 評価データセット（100文章）の拡充と精度検証

**未解決の問題**

- 特になし。単体・結合テスト40件パスおよびNext.jsビルド成功を確認済み

## 2026-09-21 サブエージェントの洗い直しで3件追加修正

**決めたこと**

- ここまでの2件の修正は自分だけで探していたので、サブエージェントに独立して全体を洗い直させた。
  見つかった3件を修正した
- (1) `/setup-matt-pocock-skills`を再実行すると、ベンダーのひな形で`docs/agents/domain.md`等を
  無条件に上書きし、今回までの穴埋めごと消える設計だった。`flow-map.md`のルールを
  「domain.mdが消えても1行で足りる」自己完結な内容に書き直し、加えて再実行時は
  現在の中身を読んで独自追記を残すよう`flow-map.md`にルール7を追加した
- (2) `handover-trim.sh`は上限を超えた古いメモを完全に削除する設計で、行き場が無かった。
  `docs/agents/handover-archive.md`へ退避してから削るよう書き直した（動作確認済み。
  会話開始時には読み込まれない保管庫）
- (3) `next-step`のドメイン文書チェックが、多コンテキスト構成の`CONTEXT-MAP.md`・
  `src/<context>/docs/adr/`を見ていなかったので追記した

**次にやること**

- `/grill-with-docs` で「何を作るか」を決める（前回から持ち越し。まだ未着手）
- 決まったら `/to-spec` → `/to-tickets` → `/implement` と進む
- `README.md` の冒頭をこのプロジェクトの説明に書き換える
- `docs/adr/`ができたら、`/code-review`実行時に実際にStandards軸へ渡るか、
  `/setup-matt-pocock-skills`を再実行しても穴埋めが残るか、両方まだ未検証

**未解決の問題**

- 今回もこの3件以外に穴が無いという保証は無い。サブエージェントの調査も
  「見た範囲では」の話であり、悉皆性の証明ではない

## 2026-09-21 フローを飛ばすと用語集・ADRが読まれない穴も修正

**決めたこと**

- 前回の`/code-review`修正は個別の穴を塞いだだけで、根はもっと広かった。
  `CONTEXT.md`・`docs/adr/`を読む指示は`/to-spec`等の個別スキルにしか書かれておらず、
  利用者が「そのまま作って」とフローを飛ばすと、その指示ごと消えて何も読まれない構成だった
- `docs/agents/domain.md`に「フローを飛ばしても読む」旨を明記し、`flow-map.md`（毎回自動読込）の
  ルール4にも同じ義務を追記した。`next-step`スキルも、`CONTEXT.md`/`docs/adr/`の**有無**しか
  見ていなかったのを、**中身を読んで食い違いがあれば一言伝える**よう直した

**次にやること**

- `/grill-with-docs` で「何を作るか」を決める（前回から持ち越し。まだ未着手）
- 決まったら `/to-spec` → `/to-tickets` → `/implement` と進む
- `README.md` の冒頭をこのプロジェクトの説明に書き換える
- 実際に `CONTEXT.md`・`docs/adr/` ができた後、フローを飛ばして直接実装を頼んでみて、
  今回の修正どおり読まれるか確認する（まだ未検証）

**未解決の問題**

- 今回も指示書を足しただけで、実地確認はしていない。`docs/adr/`が今も0件なので、
  「食い違いを検知して伝える」動作は一度も実際には動いていない

## 2026-09-21 ADRが検査されない欠陥を修正

**決めたこと**

- `/code-review` は `docs/adr/` を一切見ておらず、Standards担当のサブエージェントは
  事前に貼り付けた文書しか見えない隔離構成だった。ADRを書いても検査経路が無く、
  意味の無い記録になっていた
- `/code-review` 本体（vendored、編集禁止）は直さず、`docs/agents/domain.md` に橋渡し手順を追記し、
  `docs/agents/flow-map.md`（毎回自動読込）にも同じ趣旨のルールを追記して塞いだ

**次にやること**

- `/grill-with-docs` で「何を作るか」を決める（前回から持ち越し。まだ未着手）
- 決まったら `/to-spec` → `/to-tickets` → `/implement` と進む
- `README.md` の冒頭をこのプロジェクトの説明に書き換える
- 実際に `docs/adr/` にADRができた後、一度 `/code-review` を回して、
  今回の修正どおりADRがStandards軸に渡っているか確認する（まだ未検証）

**未解決の問題**

- 今回の修正は指示書（domain.md / flow-map.md）を足しただけで、
  `/code-review` を実際に走らせて動作確認はしていない


## テンプレートから作成

**決めたこと**

- この置き場所はテンプレートから複製したもの。開発の段取り（案内役・開始と終了の儀式・引き継ぎメモ）
  だけが入っており、製品のコードはまだ1行も無い
- 何を作るかはまだ決まっていない

**次にやること**

- `/grill-with-docs` で「何を作るか」を決める。私が質問を重ねるので、答えるだけでよい
- 決まったら `/to-spec` → `/to-tickets` → `/implement` と進む
- `README.md` の冒頭をこのプロジェクトの説明に書き換える（この1件も、書き換えたら消してよい）

**未解決の問題**

- 特になし
